class TestMaven {
     def package
     
     def test(){
           sh " echo test maven"
     }
}

class TestNodeJS {
     def package
     
     def test(){
           sh " echo test nodejs"
     }
}

def instancia(buildInfo){
     if (buildInfo.tipoProjetoMaven){
         return new TestMaven(package: buildInfo.package)
     }else if (isNode){ 
         return new TestNodeJS(package: buildInfo.package)
     }
}

return this
