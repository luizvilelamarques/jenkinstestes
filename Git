class Git {
    def credentials
    def url
    def branch
    
    def checkout() {
        sh "echo luiz"
        //gitee branch: branch, credentialsId: credentials, url: url
    }
}

def instancia(gitInfo, branch){
  return new Git(credentials: gitInfo.credentials, url: gitInfo.url, branch: branch)
}

return this
